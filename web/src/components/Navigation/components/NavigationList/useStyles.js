import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  navigationLink: {
    textDecoration: 'none',
    color: 'white',
  },
  navigationIcon: {
    color: 'white',
  },
}));
